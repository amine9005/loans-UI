
import { UserAvatarComponent } from './user-avatar.component';

describe('UserAvatarComponent', () => {
  let fixture: UserAvatarComponent;

  beforeEach(async () => {

    fixture = new UserAvatarComponent

  });

  test('should be created', () => {
    expect(fixture).toBeDefined();
  });
});
