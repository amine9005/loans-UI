// import { AuthGuard } from './auth.guard';

describe('AuthGuard', () => {
  // let fixture: AuthGuard;

  beforeEach(() => {
    //  fixture = new AuthGuard();
  });

  it('should be created', () => {
    expect(1).toBeTruthy();
  });
});
